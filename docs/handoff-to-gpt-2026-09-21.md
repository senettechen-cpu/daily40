# Claude → GPT 工作請求（2026-09-21）

由使用者轉達。依分工：GPT 負責遊戲／美術設計，Claude 負責技術與實作；以下每項都不要求 GPT 改程式。

## A. 同步使用者已改定的經濟規則（優先）

使用者已逐項確認新的收入規則，見 `docs/v1.5-economy-decisions.md`，它優先於 v1.5 第 2–3 節：

- 記帳：每筆 +20，每天前 3 筆（依記錄當天、台灣時間）；刪除會扣回。**每日核帳 +10 與核帳功能都已取消。**
- 今日核心：最多 3 個，各 +10；可前一晚預設；09:00 截止；09:00 後只能替換，數量上限為 09:00 時已勾選的數量。
- 專案：不限數量，每案最多 120（手動指定 3 個里程碑各 +20，結案 +60 且須在建立日之後才發）。
- 舊腐化、減員與滅絕令鎖畫面凍結；舊 RP／榮耀轉唯讀歷史。

請求：
1. 更新你負責的設計文件，讓它們不再描述已取消的核帳：v1.5 規格第 2–3 節、`progression-review.html`、主規格第 15 節、`life-rewards-integration.md`，以及飛昇交接 `ASCENSION_HANDOFF.md` 第 4 節第 3 點（「一般核帳可領既定軍需」）。
2. 原本以核帳為條件的兩個設計需要重新提案：
   - **每日正式戰役門檻**：v1.5 原為「一項核心＋核帳」。
   - **失序機制**：取代已凍結的腐化系統，只在預定核心成果到期未完成時每日結算一次。
   請各給 1–2 個選項，交使用者決定。

## B. 飛昇交接 v1：已收到，一個規則問題

`organ-catalog.json`、五階段、驗收清單已足以開發；依使用者決定，飛昇排在第一次上線之後。

請確認：成長紀錄「當日候選人與成果分配事先確定」的**截止時間**，是否比照今日核心的 09:00？另外，今日核心和成長紀錄能否是同一個任務？（軍需與紀錄分開計算，技術上兩種做法都可以。）

## C. 掩體／動作美術 v4：素材介面回覆與試做請求

技術回覆在 `docs/cover-animation-tech-plan.md`。座標已核對無變動，並已加入鎖定測試。

請審閱第 4 節的素材介面，同意或提出修改：

- 序列幀，加上獨立的武器圖層（不採骨架動畫）。
- 四個等角方向各自繪製，不做鏡像。
- 每幀附錨點 JSON：地面、肩、雙手握點、槍套、背帶、受擊點；武器圖另附握把與槍口錨點。
- 沙包拆成 `back` 與 `front` 兩層，另附站位點、越頂射擊點、足跡。
- **單幀畫布**：Claude 建議 192×192（人物約 120px），不用 256×256。472 幀在 256 畫布下解碼後約需 124 MB，手機風險高；192 畫布加裁切約 31 MB。請從美術品質角度評估；最後由使用者定案。

介面確認後，請先交付**試做包**，驗證後才量產：

1. 一名卡迪安士兵，**SE 方向**（我方面向敵人的主要方向），交接第 7 節全部動作：待機、移動、入掩體、探身、射擊、回掩體、換匣、主→副、副→主、手槍射擊、受擊、倒地。
2. 雷射步槍與雷射手槍的獨立武器圖，含握把與槍口錨點。
3. 一段沙包的 `back`／`front` 分層圖與 JSON。

叛變守軍、其餘方向與其他兵種，等試做包通過截圖驗收後再擴充。
